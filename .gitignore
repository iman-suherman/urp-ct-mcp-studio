node_modules/
out/
*.vsix
.DS_Store
website/node_modules/
website/.next/
releases/*
!releases/.gitkeep
!releases/notes/
!releases/notes/**
logs/*
!logs/.gitkeep

# Local environment (created by npm run generate-env — do not commit)
.env

# Project-local Google ADC (created by npm run login — do not commit)
.gcloud/application_default_credentials.json

# Cursor IDE (local only)
.cursor/mcp.json
.cursor/rules/*
!.cursor/rules/*.mdc
.env.mcp

# VS Code (local only)
.vscode/settings.json
