{
  "name": "ct-mcp-registry-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Run API for Commerce MCP plugin version history (Firestore)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "express": "^4.21.2"
  }
}
